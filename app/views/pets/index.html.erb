<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Pooch</title>
    <!--The link for our css called listing.css-->
    <link type="text/css" rel="stylesheet" href="listing.scss"/>
  </head>
  <body>
    <h1>Pet Listing</h1>
    <hr>
    <div class="petlisting">
    <table>
      <thead>
        <tr>
          <th><%= sortable "name" %></th>
          <th><%= sortable "breed" %></th>
          <th><%= sortable "size" %></th>
          <th><%= sortable "age" %></th>
          <th><%= sortable "zipcode" %></th>
          <!-- <th>Personality</th> -->
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @pets.each do |pet| %>
          <tr>
            <td><%= pet.name %></td>
            <td><%= pet.breed %></td>
            <td><%= pet.size %></td>
            <td><%= pet.age %></td>
            <td><%= pet.zipcode %></td>
            <!-- <td><%# @pet.personality.trait %></td> -->
            <td class="action"><%= link_to 'Show', pet %></td>
            <td class="action"><%= link_to 'Edit', edit_pet_path(pet) %></td>
            <td class="action"><%= link_to 'Destroy', pet, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <br>
    <div class="btn btn-lg btn-default">
      <%= link_to 'New Pet', new_pet_path %>
    </div>



</body>

<p id="notice"><%= notice %></p>
